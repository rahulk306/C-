#include<iostream>
using namespace std;
int main()
{
    // int a,b;
    // cout << "Enter the value of a and b: ";
    // cin >> a >> b;
    // cout << a << " " << b ;   

    int a;
    a = cin.get(); // this is used to scan the character so if  you enter 1 then it scanf 1 as a char and then acc to its ascii value assigned it will store that in int var a.  
    cout << a; 
}